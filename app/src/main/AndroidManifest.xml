<?xml version="1.0" encoding="utf-8"?>
<manifest package="br.com.borges.moises.expensetracker"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".accounts.AccountsActivity"
                  android:label="@string/accounts_activity"
                  android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".DashboardActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
            android:name=".ExpensesActivity"
            android:label="@string/expenses_activity"
            android:theme="@style/AppTheme">
            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            -->
        </activity>
        <activity
            android:name=".ExpenseActivity"
            android:parentActivityName=".ExpensesActivity"
            android:label="@string/expense_activity">
        </activity>
    </application>

</manifest>
